import React from 'react';

const Contacts = () => (
  <section id="contacts" style={{ height: "100vh", backgroundColor: "#999" }}>
    <h1>Contacts</h1>
    <p>Content for Main section.</p>
  </section>
);

export default Contacts;
